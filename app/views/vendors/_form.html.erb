<%= form_for (@vendor) do |f| %>
  <% if @vendor.errors.any? %>
    <div class="grid wrap">
      <div class="unit wwhole error">
        <h2><%= pluralize(@vendor.errors.count, "error") %> prohibited this job from being saved:</h2>
        <ul>
          <% @vendor.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="grid wrap">
    <div class="unit one-fifth">
      <%= f.label :name, "Name" %>
    </div>
    <div class="unit four-fifths">
      <%= f.text_field :name, :id => "name" %>
    </div>
    <div class="unit one-fifth">
      <%= f.label :website, "Website" %><br>
    </div>
    <div class="unit four-fifths">
      <%= f.text_field :website, :id => "website" %>
    </div>
    <div class="unit one-fifth">
      <%= f.label :description, "Description" %><br>
    </div>
    <div class="unit four-fifths">
      <%= f.text_area :description, :id => "description" %>
    </div>
  </div> 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
