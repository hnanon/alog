<%= form_for (@carrier) do |f| %>
  <% if @carrier.errors.any? %>
    <div class="grid wrap">
      <div class="unit whole error">
        <h2><%= pluralize(@carrier.errors.count, "error") %> prohibited this job from being saved:</h2>
        <ul>
          <% @carrier.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="grid wrap no-gutters">
    <div class="unit whole field">
      <%= f.text_field :name, :id => "name" %>
    </div>
    <div class="unit whole actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
