<div class="nested-fields">
  <div class="unit whole field">
    <%= collection_select( :product, :option_id, @options, :id, :name, {:prompt => "Select an Option"} ) %>
  </div>  <%= link_to_remove_association "-", f %>
</div>